#pragma once

using namespace std;

class Util{
	//FloatBuffer createFloatBuffer(int size);
	//IntBuffer createIntBuffer(int size);
	//IntBuffer createFlippedBuffer();
	//FloatBuffer createFlippedBuffer();
	//FloatBuffer createFlippedBuffer(Matrix4f value);
	//string removeEmptyStrings(string data);
	//int toIntArray();
};